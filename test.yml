- hosts: localhost
  gather_facts: false
  tasks:
    - name: Test my module
      my_namespace.my_collection.my_own_module:
        path: /tmp/testfile.txt
        content: "Hello from my module!"
      register: result

    - debug:
        var: result
